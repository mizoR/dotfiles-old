## Install dotfiles
# install
$ cd ~
$ git clone https://mizoR@github.com/mizoR/dotfiles.git
$ cd dotfiles
$ git submodule init
$ git submodule update
$ ./setup.sh
$ vim +BundleInstall! +q

## For ruby
# install ruby-build
$ PREFIX=$HOME/local ./ruby-build/install.sh

# install ruby
$ ruby-build --definitions
$ rbenv install [ruby-version]
$ rbenv global [ruby-version]
$ rbenv rehash
